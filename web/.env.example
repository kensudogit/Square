# フロントに埋め込まれる値のみ。
# ACCESS_TOKEN と WEBHOOK_SIGNATURE_KEY には絶対に VITE_ を付けないこと
# （VITE_ 接頭辞のものはビルド成果物に含まれ、公開される）
#
# 未設定の場合は起動時に GET /api/config から取得するので、通常は空のままでよい。
VITE_SQUARE_ENVIRONMENT=sandbox
VITE_SQUARE_APPLICATION_ID=
VITE_SQUARE_LOCATION_ID=
